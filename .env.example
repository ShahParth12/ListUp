# PostgreSQL connection string (Neon, Render Postgres, local Docker, etc.)
# Hosted DBs usually need TLS, e.g. append ?sslmode=require
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require"

# Optional: AI category validation
# OPENAI_API_KEY=
